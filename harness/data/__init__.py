"""Layer 5 — Data / Storage: the only layer that persists.

Session memory (UserStore), checkpoint records (LangGraph checkpointer), and the
seed inventory the agents read from for the offline demo.
"""
